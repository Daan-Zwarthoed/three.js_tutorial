import Head from "next/head";
import * as THREE from "three";
import Router from "next/router";
const scene = new THREE.Scene();
import React, { useEffect, useState } from "react";
import Scene from "../components/Scene";
const Tutorial = () => {
  //   const [initialPos, setInitialPos] = useState<number | null>(null);
  //   const [initialSize, setInitialSize] = useState<number | null>(null);
  let initialPos: number | null = null;
  let initialSize: number | null = null;

  const initial = (e: any) => {
    let resizable = document.getElementById("Resizable");
    if (!resizable) return;
    initialPos = e.clientY;
    initialSize = resizable.offsetHeight;
    console.log("test1");

    // let canvas = document.getElementById("canvas");
    // canvas!.style.height = "400px";
  };

  const resize = (e: any) => {
    console.log("test");

    let resizable = document.getElementById("Resizable");
    if (!resizable || !initialSize || !initialPos) return;
    resizable.style.height = `${initialSize + (e.clientY - initialPos)}px`;
  };

  return (
    <>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <div className="flex h-screen flex tablet:flex-col">
        <div id="Resizable" className="flex w-full h-full">
          hey
        </div>
        <div
          className="flex w-full h-10 bg-blue-500"
          onDragStart={(event) => initial(event)}
          onDrag={(event) => resize(event)}
        ></div>
        <Scene></Scene>
      </div>
    </>
  );
};

export default Tutorial;
